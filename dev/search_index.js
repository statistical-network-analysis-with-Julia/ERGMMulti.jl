var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/types/#Types","page":"Types","text":"MultilayerNetwork{D}, MultiERGMModel{D} and MultiERGMResult{D} carry the directedness of the layers as a type parameter, exactly as Network{T,D} and ERGM.ERGMModel{T,D} do; is_directed reads it.","title":"Types"},{"category":"section","location":"api/types/#Construction-and-conversion","page":"Types","text":"","title":"Construction and conversion"},{"category":"type","location":"api/types/#ERGMMulti.MultilayerNetwork","page":"Types","text":"MultilayerNetwork{D}\nMultilayerNetwork(n::Int; directed::Bool=true) -> MultilayerNetwork{directed}\n\nSeveral relations (\"layers\") on the same actor set. All layers share the number of actors and the directedness D, which is a type parameter (as it is for Network{T,D}): layers is a concretely typed Vector{Network{Int,D}}, is_directed(m) reads D, and a layer of the other directedness is refused by add_layer!.\n\nFields\n\nn::Int: Number of actors\nlayers::Vector{Network{Int,D}}: One network per layer\nlayer_names::Vector{Symbol}\n\nExample\n\nusing ERGMMulti, Networks\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :friendship)\nadd_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2)\nis_directed(m)                       # true\nm isa MultilayerNetwork{true}        # true\neltype(m.layers) === Network{Int,true}   # true\nlayer_names(m)                       # [:friendship, :advice]\n\n\n\n\n\n","title":"ERGMMulti.MultilayerNetwork"},{"category":"type","location":"api/types/#ERGMMulti.MultiNetwork","page":"Types","text":"MultiNetwork(networks::Vector{<:Network}, names::Vector{Symbol})\n\nA collection of independent networks (possibly of different sizes and directedness), for descriptive pooling with CrossNetEdges. It never enters a fit: ergm.multi's covariate-driven Networks() models (N(~edges, ~x)) are not implemented, and ergm_multi accepts a MultilayerNetwork only.\n\nExample\n\nusing ERGMMulti, ERGM, Networks\na = network(4; directed=false); add_edge!(a, 1, 2)\nb = network(6; directed=false); add_edge!(b, 1, 2); add_edge!(b, 3, 4)\nmn = MultiNetwork([a, b], [:school1, :school2])\nlength(mn)                                    # 2\ncompute(CrossNetEdges(), mn)                  # 3.0 — edges pooled over the networks\n\n\n\n\n\n","title":"ERGMMulti.MultiNetwork"},{"category":"type","location":"api/types/#ERGMMulti.MultilevelNetwork","page":"Types","text":"MultilevelNetwork\n\nA hierarchical design: one network per level plus membership maps and explicit cross-level edges. The associated statistics (Nestedness, CrossLevelEdge, LevelHomophily) are descriptive.\n\nFields\n\nlevel_nets::Vector{Network{Int}}: One network per level\nmembership::Vector{Dict{Int,Int}}: membership[l][node] = group, the level-(l+1) unit that level-l node node belongs to\ncross_level_edges::Vector{NTuple{4,Int}}: (level_from, node_from, level_to, node_to) ties added via add_cross_level_edge!\n\nExample\n\nusing ERGMMulti, ERGM, Networks\npeople = network(5; directed=false)\nadd_edge!(people, 1, 2); add_edge!(people, 2, 3); add_edge!(people, 3, 4)\norgs = network(2; directed=false)\nmembership = [Dict(1 => 1, 2 => 1, 3 => 2, 4 => 2, 5 => 2)]   # person => organisation\nml = MultilevelNetwork([people, orgs], membership)\ncompute(LevelHomophily(1), ml)      # 2.0 — (1,2) and (3,4) stay inside one organisation\ncompute(Nestedness(1), ml)          # 0.666… — 2 of the 3 person ties\n\n\n\n\n\n","title":"ERGMMulti.MultilevelNetwork"},{"category":"type","location":"api/types/#ERGMMulti.MultiERGMModel","page":"Types","text":"MultiERGMModel{D}\nMultiERGMModel(terms, m::MultilayerNetwork{D}; offsets=Dict{Int,Float64}())\n\nMultilayer ERGM specification: terms, data, and any per-term offsets. D is the directedness of the network (is_directed(model)), as for ERGM.ERGMModel{T,D}.\n\nThe constructor validates the formula against the data before anything is fitted, throwing an ArgumentError that names the fix:\n\nevery layer a term refers to (LayerEdges(3), InterlayerDependence(1, 3), WithinLayer(t, 3)) must exist — the message lists layer_names(m);\nevery WithinLayer(t, l) is checked against layer l with ERGM.jl's own formula validator (ERGM._validate_formula): a missing or partial vertex attribute, an EdgeCov of the wrong size, and an undirected-only term (Kstar, GWDegree, Degree) on a directed layer or a directed-only one (Mutual, OStar, ...) on an undirected layer are refused;\nLayerMutual and MultiplexMutual are directed-only (requires_directed) and are refused on a MultilayerNetwork{false};\na bare ERGM.jl term (Mutual() where WithinLayer(Mutual(), l) was meant — ergm.multi's L(~mutual, ~A)) or a multilevel descriptive (Nestedness) has no multilayer change statistic and is refused with the WithinLayer fix spelled out (it used to fail at fit time with a MethodError);\nno layer may contain a self-loop (a loops=true layer with none is fine): the statistics would count it but the within-layer design, nobs, the sampler and gof never touch the diagonal — the same refusal as ERGM.ERGMModel's (\"R ergm warns \"This network contains loops\" here\");\nthe network must have at least one layer and at least two actors — a MultilayerNetwork(0) or MultilayerNetwork(1) has no within-layer dyad, so there is nothing to estimate or simulate (it used to run Newton on an empty design and return coef = [0.0], NaN standard errors and two misleading non-convergence warnings);\noffsets must index terms (1:length(terms)), an offset term must not expand into several statistics (pin each level/degree with its own single-statistic term instead), and at least one coefficient must be free.\n\n(A layer selected by name inside a term — LayerEdges(:advice) — is an ArgumentError at the term's construction, before the model: terms carry indices, and the message gives findfirst(==(:advice), layer_names(m)).)\n\nAttribute terms inside WithinLayer are then materialized (ERGM._materialize), so the stored terms — an ERGM.TermSet whose names are the direction-aware coefficient labels (name(term, m)) — may hold more statistics than terms were given (a multi-level NodeFactor, a Degree(0:2)); offsets keys are remapped to the expanded positions.\n\nFields\n\nterms::ERGM.TermSet: the (materialized, expanded) statistics and their labels\nnetwork::MultilayerNetwork{D}\noffsets::Dict{Int,Float64}: fixed coefficients, keyed by statistic index\n\nExample\n\nusing ERGMMulti, ERGM, Networks\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nmodel = MultiERGMModel([LayerEdges(1), WithinLayer(GWESP(0.5), 2)], m)\nmodel.terms.names          # [\"L.edges.1\", \"L2.gwesp.OTP.fixed.0.5\"]\nis_directed(model)         # true\ntry\n    MultiERGMModel([LayerEdges(3)], m)\ncatch e\n    occursin(\"layer 3\", e.msg) && occursin(\":advice\", e.msg)   # true\nend\n\n\n\n\n\n","title":"ERGMMulti.MultiERGMModel"},{"category":"type","location":"api/types/#ERGMMulti.MultiERGMResult","page":"Types","text":"MultiERGMResult{D}\n\nResults from ergm_multi on a MultilayerNetwork{D}. Offset terms report their fixed coefficient with NaN standard error (and NaN rows/columns of vcov); loglik is the maximized pseudo-log-likelihood over the within-layer dyads.\n\nse_type records how std_errors/vcov were ACTUALLY obtained — :hessian (the inverse negative pseudo-Hessian, anticonservative under dependence) or :bootstrap (the parametric bootstrap of ergm_multi(...; se=:bootstrap)). It is what Networks.se_method(fit) reports, and what show reads before deciding whether the anticonservatism caveat still applies. Offset rows stay NaN under either option: a fixed coefficient carries no uncertainty.\n\nboot_replicates is the n_boot × p_free matrix of refitted free coefficients under se=:bootstrap (a replicate whose refit did not converge is a NaN row, excluded from the covariance), nothing otherwise.\n\nconverged is false when the Newton iteration did not meet its tolerance or when the pseudo-likelihood has no finite maximum by perfect separation (separated == true, R's \"The MPLE does not exist!\"): the coefficients are then the point where Newton stopped on its flat asymptote and are unreliable. A statistic at the boundary of its attainable range is handled differently — its coefficient is fixed at ∓Inf (see ergm_multi).\n\nThe full StatsAPI surface is implemented: coef, stderror, vcov, confint, loglikelihood, nobs, dof, aic, bic and coeftable (a Networks.CoefficientTable — the table show prints). The shared result-metadata accessors (is_exact, se_method, approximations, ...) read the same fields.\n\nExample\n\nusing ERGMMulti, ERGM, Networks, Random\nrng = Xoshiro(2)\nm = MultilayerNetwork(12; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nfor l in 1:2, i in 1:12, j in 1:12\n    i != j && rand(rng) < 0.25 && add_layer_edge!(m, l, i, j)\nend\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\nfit isa MultiERGMResult{true}      # true\nfit.converged, fit.separated       # (true, false)\nse_method(fit)                     # :hessian\nis_exact(fit)                      # true — every term is dyad-independent\nfit.boot_replicates === nothing    # true — no bootstrap was run\napproximations(fit)                # String[] — nothing to disclose\n\n\n\n\n\n","title":"ERGMMulti.MultiERGMResult"},{"category":"function","location":"api/types/#ERGMMulti.add_layer!","page":"Types","text":"add_layer!(m::MultilayerNetwork, name::Symbol;\n           net::Union{Network,Nothing}=nothing) -> MultilayerNetwork\n\nAdd a layer, either empty or from an existing Network with matching size and directedness. A network of the other directedness is refused with an ArgumentError naming both (a MultilayerNetwork{true} holds directed layers only, and vice versa), and so is a two-mode (bipartite) network: ERGMMulti models one-mode layers only, and a bipartite layer would have its impossible within-mode pairs enumerated as observed non-ties by the within-layer MPLE (statnet's bipartite layer terms b1dspL, ... are not implemented). A layer that allows self-loops (loops=true) is accepted here; a layer that contains one is refused by MultiERGMModel.\n\nExample\n\nusing ERGMMulti, Networks\nm = MultilayerNetwork(3; directed=false)\nadd_layer!(m, :a)                                    # empty undirected layer\nnet = network(3; directed=false); add_edge!(net, 1, 2)\nadd_layer!(m, :b; net=net)                           # from an existing network\ntry\n    add_layer!(m, :c; net=network(3; directed=true))\ncatch e\n    occursin(\"undirected\", e.msg)                    # true\nend\n\n\n\n\n\n","title":"ERGMMulti.add_layer!"},{"category":"function","location":"api/types/#ERGMMulti.add_layer_edge!","page":"Types","text":"add_layer_edge!(m::MultilayerNetwork, layer, i, j) -> MultilayerNetwork\n\nAdd the edge (i, j) in the given layer (an index or a name) — add_edge! on that layer's Network, so on an undirected MultilayerNetwork{false} the pair is unordered and a repeated edge is a no-op.\n\nAn actor id outside 1:n (a mistyped id, a 0-based id from Python data) and a self-loop (i, i) on a layer built without loops=true are refused with an ArgumentError naming the ids and the range — never silently dropped, which is what add_edge!'s false return used to become. An unknown layer name or an out-of-range layer index is an ArgumentError too (layer_network).\n\nExample\n\nusing ERGMMulti, Networks\nm = MultilayerNetwork(3; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2)          # by name\nadd_layer_edge!(m, 2, 2, 3)                    # by index\nne(layer_network(m, :friendship)), ne(layer_network(m, :advice))   # (1, 1)\nhas_edge(layer_network(m, :advice), 3, 2)      # false — directed\ntry\n    add_layer_edge!(m, :advice, 0, 2)          # 0-based id\ncatch e\n    occursin(\"actor ids must lie in 1:3\", e.msg)   # true\nend\n\n\n\n\n\n","title":"ERGMMulti.add_layer_edge!"},{"category":"function","location":"api/types/#ERGMMulti.layer_network","page":"Types","text":"layer_network(m::MultilayerNetwork, layer) -> Network\n\nThe Network{Int,D} of a layer, by index or by name — the object itself, not a copy, so attributes and missing-dyad masks set on it are seen by the model. An unknown name, or an index outside 1:n_layers, is an ArgumentError naming the layers the network has (never a raw BoundsError).\n\nExample\n\nusing ERGMMulti, Networks\nm = MultilayerNetwork(3; directed=false)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :advice, 1, 3)\nlayer_network(m, 2) === layer_network(m, :advice)     # true — the same Network\nhas_edge(layer_network(m, :advice), 3, 1)              # true — undirected\ntry\n    layer_network(m, :trust)\ncatch e\n    occursin(\"no layer named :trust\", e.msg)           # true\nend\ntry\n    layer_network(m, 5)\ncatch e\n    occursin(\"layer index 5 out of range\", e.msg)      # true\nend\n\n\n\n\n\n","title":"ERGMMulti.layer_network"},{"category":"function","location":"api/types/#ERGMMulti.layer_names","page":"Types","text":"layer_names(m::MultilayerNetwork) -> Vector{Symbol}\n\nThe layer names in index order — the mapping behind every integer layer selector (LayerEdges(2) is the layer layer_names(m)[2], and the validation errors quote this list). Returns a copy; the network's own vector is not exposed.\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(3; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nlayer_names(m)                                # [:friendship, :advice]\nfindfirst(==(:advice), layer_names(m))        # 2 — the index `LayerEdges(2)` selects\n\n\n\n\n\n","title":"ERGMMulti.layer_names"},{"category":"function","location":"api/types/#ERGMMulti.as_multilayer","page":"Types","text":"as_multilayer(nets::Vector{<:Network}, names::Vector{Symbol};\n              report=false) -> MultilayerNetwork\n\nBuild a multilayer network from same-sized networks. The directedness of the first network sets the type parameter; every other layer must match (add_layer! refuses a mismatch). The layer networks are stored as given — not copied — so their vertex/edge/network attributes and their missing-dyad masks survive inside the MultilayerNetwork unchanged: the conversion is lossless, and with report=true it returns (m, ::ConversionReport) whose is_lossless is true.\n\nExample\n\nusing ERGMMulti, Networks\na = network(4; directed=true); add_edge!(a, 1, 2); set_missing_dyad!(a, 3, 4)\nb = network(4; directed=true); add_edge!(b, 2, 3)\nm, rep = as_multilayer([a, b], [:friendship, :advice]; report=true)\nis_lossless(rep)                                   # true\nn_missing_dyads(layer_network(m, :friendship))     # 1 — the mask is kept\n\n\n\n\n\n","title":"ERGMMulti.as_multilayer"},{"category":"function","location":"api/types/#ERGMMulti.combine_networks","page":"Types","text":"combine_networks(m::MultilayerNetwork; report=false) -> Network\ncombine_networks(m; report=true) -> (Network, ConversionReport)\n\nThe block-diagonal combined network of ergm.multi's Layer() construct: one network with n × L vertices, where vertex (l-1)·n + a is actor a's copy in layer l. Each vertex carries the :layer (layer index) and :actor (original actor ID) attributes, and every layer's edges are placed within its own block. Cross-block dyads are structurally empty — the model's dyad universe is the within-block dyads only.\n\nThe missing-dyad mask is preserved: a masked (unobserved) dyad (i, j) of layer l is masked at (off + i, off + j) of the combined network (off = (l-1)·n), so n_missing_dyads(combined) is the sum over the layers and split_by_layer restores every mask. An unobserved within-layer tie never becomes an observed absent one on the way through. The loops flag is carried when any layer allows self-loops.\n\nWhat a block-diagonal network cannot hold is dropped and reported: the layers' own vertex attributes (only :layer/:actor are carried), edge attributes and network attributes. (A two-mode layer never gets this far: add_layer! refuses it.) With report=true the function returns (combined, ::ConversionReport) whose dropped_fields name each dropped attribute (and the layer it came from, in the reason), so a covariate that vanished is never silent.\n\nExample\n\nusing ERGMMulti, Networks\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2)\nset_missing_dyad!(layer_network(m, :advice), 2, 3)          # unobserved in layer 2\nset_vertex_attribute!(layer_network(m, :friendship), :grp,\n                      Dict(1 => \"A\", 2 => \"A\", 3 => \"B\", 4 => \"B\"))\nc, rep = combine_networks(m; report=true)\nnv(c), ne(c)                              # (8, 1)\nis_missing_dyad(c, 6, 7)                  # true — actor 2→3 in block 2\nn_missing_dyads(c)                        # 1\ndropped_fields(rep)                       # [:grp]\n\n\n\n\n\n","title":"ERGMMulti.combine_networks"},{"category":"function","location":"api/types/#ERGMMulti.split_by_layer","page":"Types","text":"split_by_layer(combined::Network, n::Int, L::Int;\n               names=Symbol[], report=false) -> MultilayerNetwork\n\nInverse of combine_networks: recover the L layers of n actors from a block-diagonal combined network. Every edge must lie within a block (a cross-block edge is an ArgumentError), and so must every masked dyad: the mask of block l is remapped onto layer l (is_missing_dyad(layer_l, i, j) for a masked (off + i, off + j)), and a masked cross-block dyad is refused with the same ArgumentError as a cross-block edge — a dyad the block-diagonal model declares structurally empty cannot also be unobserved. The loops flag is carried to every layer.\n\nWith report=true returns (m, ::ConversionReport): the combined network's vertex attributes other than :layer/:actor, its edge attributes and its network attributes cannot be attributed to a layer and are dropped and named.\n\nExample\n\nusing ERGMMulti, Networks\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :advice, 4, 3)\nset_missing_dyad!(layer_network(m, :advice), 2, 3)\nc = combine_networks(m)\nm2 = split_by_layer(c, 4, 2; names=[:friendship, :advice])\nhas_edge(layer_network(m2, :advice), 4, 3)              # true\nis_missing_dyad(layer_network(m2, :advice), 2, 3)       # true — round trip\n\n\n\n\n\n","title":"ERGMMulti.split_by_layer"},{"category":"function","location":"api/types/#ERGMMulti.add_cross_level_edge!","page":"Types","text":"add_cross_level_edge!(m::MultilevelNetwork, level_from, node_from,\n                      level_to, node_to)\n\nRecord a cross-level tie (e.g. a person–organization affiliation beyond the nesting structure); counted by CrossLevelEdge. A level the network does not have is an ArgumentError.\n\nExample\n\nusing ERGMMulti, ERGM, Networks\npeople = network(3; directed=false)\norgs = network(2; directed=false)\nml = MultilevelNetwork([people, orgs], [Dict(1 => 1, 2 => 1, 3 => 2)])\nadd_cross_level_edge!(ml, 1, 3, 2, 1)     # person 3 is also affiliated with organisation 1\ncompute(CrossLevelEdge(), ml)             # 1.0\ntry\n    add_cross_level_edge!(ml, 1, 1, 3, 1) # there is no level 3\ncatch e\n    occursin(\"level out of range\", e.msg) # true\nend\n\n\n\n\n\n","title":"ERGMMulti.add_cross_level_edge!"},{"category":"section","location":"api/terms/#Terms","page":"Terms","text":"","title":"Terms"},{"category":"section","location":"api/terms/#Coefficient-labels","page":"Terms","text":"Statistics are labelled as R ergm.multi labels them, resolved against the network with ERGM.jl's two-argument name(term, net) generic, so a directed layer's WithinLayer(GWESP(0.5), 1) prints as L1.gwesp.OTP.fixed.0.5 and an undirected one as L1.gwesp.fixed.0.5.","title":"Coefficient labels"},{"category":"type","location":"api/terms/#ERGMMulti.LayerEdges","page":"Terms","text":"LayerEdges(layers=:) <: AbstractERGMTerm\n\nEdge count within the selected layer(s). With a layer index this is the per-layer edges term (ergm.multi's L(~edges, ~A)); with : (or a vector) it pools the coefficient across layers — the edges statistic of the block-diagonal combined network, one coefficient for all selected layers. Dyad-independent; labelled L.edges.<sel>.\n\nExample\n\nusing ERGMMulti, ERGM\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2); add_layer_edge!(m, :friendship, 2, 3)\nadd_layer_edge!(m, :advice, 1, 2)\ncompute(LayerEdges(1), m)          # 2.0 — layer 1 only\ncompute(LayerEdges(), m)           # 3.0 — pooled over all layers\ncompute(LayerEdges([1, 2]), m)     # 3.0 — the same pool, spelled out\nname(LayerEdges(2))                # \"L.edges.2\"\n\n\n\n\n\n","title":"ERGMMulti.LayerEdges"},{"category":"type","location":"api/terms/#ERGMMulti.LayerMutual","page":"Terms","text":"LayerMutual(layers=:) <: AbstractERGMTerm\n\nMutual (reciprocated) dyads within the selected layer(s). Directed multilayer networks only: requires_directed(LayerMutual()) == true, so on a MultilayerNetwork{false} model construction, compute and change_stat_layer all throw the same ArgumentError ERGM.jl raises for Mutual() on an undirected network (it used to return 0.0 silently). ergm.multi's L(~mutual, ~A); labelled L.mutual.<sel>.\n\nExample\n\nusing ERGMMulti, ERGM\nm = MultilayerNetwork(3; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :a, 2, 1); add_layer_edge!(m, :a, 2, 3)\ncompute(LayerMutual(1), m)                 # 1.0 — the (1,2) dyad is reciprocated\ncompute(LayerMutual(), m)                  # 1.0 — pooled; layer :b is empty\nchange_stat_layer(LayerMutual(1), m, 1, 3, 2)   # 1.0 — 3→2 would reciprocate 2→3\nu = MultilayerNetwork(3; directed=false); add_layer!(u, :a)\ntry\n    compute(LayerMutual(1), u)             # refused, never a silent 0.0\ncatch e\n    occursin(\"only defined for directed networks\", e.msg)   # true\nend\n\n\n\n\n\n","title":"ERGMMulti.LayerMutual"},{"category":"type","location":"api/terms/#ERGMMulti.LayerTriangle","page":"Terms","text":"LayerTriangle(layers=:) <: AbstractERGMTerm\n\nTriangle count within the selected layer(s), using ERGM.jl's Triangle statistic per layer (ergm.multi's L(~triangle, ~A); labelled L.triangle.<sel>). Dyad-dependent, so a fit containing it carries the pseudo-likelihood caveat.\n\nExample\n\nusing ERGMMulti, ERGM\nm = MultilayerNetwork(4; directed=false)\nadd_layer!(m, :a); add_layer!(m, :b)\nfor (i, j) in ((1, 2), (2, 3), (1, 3), (3, 4))\n    add_layer_edge!(m, :a, i, j)\nend\nadd_layer_edge!(m, :b, 1, 2)\ncompute(LayerTriangle(1), m)                     # 1.0\nchange_stat_layer(LayerTriangle(1), m, 1, 2, 4)  # 1.0 — adding 2-4 closes {2,3,4}\nchange_stat_layer(LayerTriangle(1), m, 2, 2, 4)  # 0.0 — layer 2 is not selected\n\n\n\n\n\n","title":"ERGMMulti.LayerTriangle"},{"category":"type","location":"api/terms/#ERGMMulti.WithinLayer","page":"Terms","text":"WithinLayer(term, layer) <: AbstractERGMTerm\n\nLift any ERGM.jl term into a single layer: the statistic (and its add-direction change statistic) of term evaluated on that layer's network. This is the analogue of ergm.multi's Layer(~term).\n\nThe wrapped term is validated against the layer when a MultiERGMModel is built (through the public ERGM._validate_formula): a vertex attribute the layer lacks, or has on only some vertices, an EdgeCov of the wrong size, and a direction requirement the layer does not meet (Kstar, GWDegree, Degree are undirected-only; Mutual, OStar, IStar, GWODegree, ... directed-only) raise ERGM.jl's own actionable errors instead of a silent 0.0. Attribute terms are then materialized (ERGM._materialize), so their change statistics read a dense snapshot rather than a Dict per toggle, and a term that expands into several statistics (NodeFactor(:g) with several levels, Degree(0:2)) contributes one coefficient per statistic, as in ERGM.jl.\n\nThe coefficient label is \"L<layer>.\" * name(term, layer_network) — the direction-aware ERGM.jl/R label, so a directed layer's WithinLayer(GWESP(0.5), 1) is L1.gwesp.OTP.fixed.0.5 and an undirected one L1.gwesp.fixed.0.5.\n\nExample\n\nusing ERGMMulti, ERGM, Networks\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :a, 2, 1)\ncompute(WithinLayer(Mutual(), 1), m)           # 1.0\nname(WithinLayer(GWESP(0.5), 1), m)            # \"L1.gwesp.OTP.fixed.0.5\"\ntry\n    MultiERGMModel([WithinLayer(Kstar(2), 1)], m)   # undirected-only term\ncatch e\n    occursin(\"OStar(2)\", e.msg)                # true — ERGM.jl's own hint\nend\n\n\n\n\n\n","title":"ERGMMulti.WithinLayer"},{"category":"type","location":"api/terms/#ERGMMulti.InterlayerDependence","page":"Terms","text":"InterlayerDependence(l1, l2) <: AbstractERGMTerm\n\nCross-layer co-occurrence: the number of dyads (i, j) with an edge in both layers l1 and l2 (ordered dyads for directed networks). A positive coefficient means a tie in one layer predicts the same tie in the other. This is ergm.multi's L(~edges, ~A&B) — the only layer-logic conjunction implemented (see \"Not implemented\" in the README); labelled duplex.<l1>.<l2>. Dyad-dependent over the (layer, i, j) universe.\n\nExample\n\nusing ERGMMulti, ERGM\nm = MultilayerNetwork(3; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2); add_layer_edge!(m, :friendship, 2, 3)\nadd_layer_edge!(m, :advice, 1, 2);     add_layer_edge!(m, :advice, 3, 2)\ncompute(InterlayerDependence(1, 2), m)                     # 1.0 — only 1→2 is in both\nchange_stat_layer(InterlayerDependence(1, 2), m, 1, 3, 2)  # 1.0 — 3→2 exists in :advice\nname(InterlayerDependence(1, 2))                           # \"duplex.1.2\"\n\n\n\n\n\n","title":"ERGMMulti.InterlayerDependence"},{"category":"type","location":"api/terms/#ERGMMulti.MultiplexMutual","page":"Terms","text":"MultiplexMutual(l1, l2) <: AbstractERGMTerm\n\nCross-layer reciprocity: the number of ordered dyads (i, j) with i→j in layer l1 and j→i in layer l2 (e.g. friendship reciprocated by advice). Directed networks only: requires_directed(MultiplexMutual(1, 2)) == true, and on a MultilayerNetwork{false} model construction, compute and change_stat_layer all throw ERGM.jl's directed-only ArgumentError (it used to return 0.0 silently). This is ergm.multi's mutualL(Ls = list(~A, ~B)); labelled duplex.mutual.<l1>.<l2>.\n\nExample\n\nusing ERGMMulti, ERGM\nm = MultilayerNetwork(3; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2)      # 1 names 2 a friend ...\nadd_layer_edge!(m, :advice, 2, 1)          # ... and 2 seeks 1's advice\ncompute(MultiplexMutual(1, 2), m)          # 1.0\ncompute(InterlayerDependence(1, 2), m)     # 0.0 — different dyads, no co-occurrence\nu = MultilayerNetwork(3; directed=false); add_layer!(u, :a); add_layer!(u, :b)\ntry\n    MultiERGMModel([MultiplexMutual(1, 2)], u)\ncatch e\n    occursin(\"only defined for directed networks\", e.msg)   # true\nend\n\n\n\n\n\n","title":"ERGMMulti.MultiplexMutual"},{"category":"type","location":"api/terms/#ERGMMulti.CrossNetEdges","page":"Terms","text":"CrossNetEdges <: AbstractERGMTerm\n\nTotal edge count across the networks of a MultiNetwork (descriptive pooling over independent networks; it does not enter a fit).\n\nExample\n\nusing ERGMMulti, ERGM, Networks\na = network(4; directed=false); add_edge!(a, 1, 2)\nb = network(6; directed=true);  add_edge!(b, 1, 2); add_edge!(b, 3, 4)\ncompute(CrossNetEdges(), MultiNetwork([a, b], [:school1, :school2]))   # 3.0\n\n\n\n\n\n","title":"ERGMMulti.CrossNetEdges"},{"category":"type","location":"api/terms/#ERGMMulti.Nestedness","page":"Terms","text":"Nestedness(level) <: AbstractERGMTerm\n\nDescriptive: the proportion of level-level edges whose endpoints share the same higher-level unit (0.0 when the level has no edge between nodes with a recorded membership). Never enters a fit; level must have a parent level.\n\nExample\n\nusing ERGMMulti, ERGM, Networks\npeople = network(4; directed=false)\nadd_edge!(people, 1, 2); add_edge!(people, 2, 3); add_edge!(people, 3, 4)\norgs = network(2; directed=false)\nml = MultilevelNetwork([people, orgs], [Dict(1 => 1, 2 => 1, 3 => 2, 4 => 2)])\ncompute(Nestedness(1), ml)          # 0.666… — (1,2) and (3,4) are within-organisation\n\n\n\n\n\n","title":"ERGMMulti.Nestedness"},{"category":"type","location":"api/terms/#ERGMMulti.CrossLevelEdge","page":"Terms","text":"CrossLevelEdge <: AbstractERGMTerm\n\nDescriptive: the number of explicit cross-level edges (added with add_cross_level_edge!). Never enters a fit.\n\nExample\n\nusing ERGMMulti, ERGM, Networks\nml = MultilevelNetwork([network(3; directed=false), network(2; directed=false)],\n                       [Dict(1 => 1, 2 => 1, 3 => 2)])\ncompute(CrossLevelEdge(), ml)             # 0.0\nadd_cross_level_edge!(ml, 1, 3, 2, 1)\ncompute(CrossLevelEdge(), ml)             # 1.0\n\n\n\n\n\n","title":"ERGMMulti.CrossLevelEdge"},{"category":"type","location":"api/terms/#ERGMMulti.LevelHomophily","page":"Terms","text":"LevelHomophily(level) <: AbstractERGMTerm\n\nDescriptive: the number of level-level edges between nodes belonging to the same higher-level unit. Membership is looked up by the level's own node IDs (a missing membership excludes the edge). Never enters a fit; Nestedness is the same count as a proportion.\n\nExample\n\nusing ERGMMulti, ERGM, Networks\npeople = network(4; directed=false)\nadd_edge!(people, 1, 2); add_edge!(people, 2, 3); add_edge!(people, 3, 4)\norgs = network(2; directed=false)\nml = MultilevelNetwork([people, orgs], [Dict(1 => 1, 2 => 1, 3 => 2, 4 => 2)])\ncompute(LevelHomophily(1), ml)      # 2.0 — (1,2) and (3,4); (2,3) crosses organisations\n\n\n\n\n\n","title":"ERGMMulti.LevelHomophily"},{"category":"function","location":"api/terms/#ERGMMulti.change_stat_layer","page":"Terms","text":"change_stat_layer(term, m::MultilayerNetwork, l, i, j) -> Float64\n\nAdd-direction change statistic for adding edge (i,j) in layer l, holding every other (layer, dyad) fixed. Must not depend on the dyad's own current state (the multilayer analogue of ERGM.jl's change_stat convention; the tests verify every term against brute-force recomputation). The MPLE design has one row of these per within-layer dyad, and the Metropolis sampler evaluates them once per proposal.\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(3; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :b, 1, 2)\nchange_stat_layer(LayerEdges(1), m, 1, 1, 2)                # 1.0 — an edge in layer 1\nchange_stat_layer(LayerEdges(1), m, 2, 1, 2)                # 0.0 — layer 2 is not selected\nchange_stat_layer(InterlayerDependence(1, 2), m, 1, 1, 2)   # 1.0 — 1→2 is already in layer 2\nchange_stat_layer(InterlayerDependence(1, 2), m, 1, 2, 1)   # 0.0\n\n\n\n\n\n","title":"ERGMMulti.change_stat_layer"},{"category":"method","location":"api/terms/#Networks.name-Tuple{WithinLayer, MultilayerNetwork}","page":"Terms","text":"name(t::WithinLayer, m::MultilayerNetwork) -> String\n\nThe coefficient label of a within-layer term on m: \"L<layer>.\" followed by the wrapped term's label on that layer's network (name(term, net)), so it agrees with ERGM.jl and R on direction-dependent labels.\n\n\n\n\n\n","title":"Networks.name"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"Construct aligned layers, inspect layer statistics, and fit pooled or layer-specific parameters. The simulation example shows what a fitted model generates; the estimation guide explains the limits of MPLE uncertainty for dependent layers.\n\nnote: Before you begin\nFitting accepts one-mode layers with the same actors and directedness. Estimation is MPLE; MCMC-MLE is not implemented. Multilevel and multiple-network containers have descriptive uses but are not alternate fitted model families. See Not implemented for the precise boundary.","title":"Getting Started"},{"category":"section","location":"getting_started/#Installation","page":"Getting Started","text":"<p>Use Julia <strong>1.12 or newer</strong> and the <a href=\"/getting-started/\">shared workspace installation guide</a>. These development packages are not yet registered; the guide prepares the required sibling checkouts and a Julia environment for the examples.</p>\n\nRun the blocks below in order in that environment. They build on variables from earlier steps; stochastic examples use seeded random number generators where shown.","title":"Installation"},{"category":"section","location":"getting_started/#Building-a-multilayer-network","page":"Getting Started","text":"using ERGMMulti, Networks\nusing ERGM: compute, Triangle   # generic evaluator + terms shared with ERGM.jl\n\nm = MultilayerNetwork(30; directed = true)   # a MultilayerNetwork{true}\nadd_layer!(m, :friendship)\nadd_layer!(m, :advice)\n\nadd_layer_edge!(m, :friendship, 1, 2)\nadd_layer_edge!(m, :advice, 1, 2)\n\n# Or from existing same-sized networks (the first one's directedness sets D;\n# a layer of the other directedness is refused)\nfriend_net = network(30; directed = true)\nadvice_net = network(30; directed = true)\nm = as_multilayer([friend_net, advice_net], [:friendship, :advice])\nis_directed(m)                               # true","title":"Building a multilayer network"},{"category":"section","location":"getting_started/#Statistics","page":"Getting Started","text":"compute(LayerEdges(1), m)                    # per-layer edges\ncompute(LayerEdges(), m)                     # pooled over layers\ncompute(InterlayerDependence(1, 2), m)       # co-occurring dyads\ncompute(WithinLayer(Triangle(), 1), m)       # any ERGM term, one layer","title":"Statistics"},{"category":"section","location":"getting_started/#Fitting","page":"Getting Started","text":"fit_ergm_multi and ergm_multi are the same function (the ecosystem's fit_<model> name and the R name). The terms are validated against the layers before anything is fitted: an out-of-range layer index, a vertex attribute a layer lacks, or an undirected-only term on a directed layer throw an ArgumentError that names the fix.\n\nusing Random\nrng = Xoshiro(1)\nfor i in 1:30, j in 1:30\n    i == j && continue\n    rand(rng) < 0.1 && add_layer_edge!(m, :friendship, i, j)\n    rand(rng) < 0.1 && add_layer_edge!(m, :advice, i, j)\nend\n\nresult = fit_ergm_multi(m, [LayerEdges(), InterlayerDependence(1, 2)])\nprintln(result)\ncoeftable(result)          # the printed table, inspectable\nconfint(result)            # Wald limits, one row per coefficient\n\n# Per-layer parameterization\nresult = ergm_multi(m, [LayerEdges(1), LayerEdges(2),\n                        InterlayerDependence(1, 2)])\n\n# Offsets fix coefficients (ergm.multi's offset mechanism)\nresult = ergm_multi(m, [LayerEdges(), InterlayerDependence(1, 2)];\n                    offsets = Dict(1 => -log(30)))","title":"Fitting"},{"category":"section","location":"getting_started/#Simulation","page":"Getting Started","text":"draws = simulate_multi_ergm(m, [LayerEdges(), InterlayerDependence(1, 2)],\n                            [-1.5, 2.0]; n_sim = 100, rng = Xoshiro(2))\n\nThe sampler only toggles within-layer dyads, matching the block-diagonal dyad universe; it runs on the ERGM family's one Metropolis kernel (ERGM.mh_toggle!), and burnin/interval default to the family's dyad-scaled rule.","title":"Simulation"},{"category":"section","location":"guide/terms/#terms-guide","page":"Terms","text":"All multilayer terms implement compute(term, m) and change_stat_layer(term, m, l, i, j) — the add-direction change in the statistic when edge (i, j) is added in layer l, independent of that dyad's current state (the multilayer analogue of ERGM.jl's change-statistic convention). The tests verify every change statistic against brute-force recomputation on a directed and an undirected fixture.\n\nusing ERGMMulti, ERGM\nm = MultilayerNetwork(4; directed = true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2); add_layer_edge!(m, :friendship, 2, 1)\nadd_layer_edge!(m, :advice, 1, 2);     add_layer_edge!(m, :advice, 2, 3)\ncompute(LayerEdges(1), m)                                  # 2.0\ncompute(LayerMutual(1), m)                                 # 1.0\ncompute(InterlayerDependence(1, 2), m)                     # 1.0 — 1→2 is in both\nchange_stat_layer(InterlayerDependence(1, 2), m, 1, 2, 3)  # 1.0 — 2→3 exists in :advice","title":"Terms"},{"category":"section","location":"guide/terms/#Layer-selectors","page":"Terms","text":"LayerEdges, LayerMutual, and LayerTriangle accept an Int (one layer), a Vector{Int}, or : (all layers) — vectors and : pool the coefficient across layers. A pooled term is the statistic of the block-diagonal combined network (the sum over the selected layers under one coefficient).","title":"Layer selectors"},{"category":"section","location":"guide/terms/#Within-layer-terms","page":"Terms","text":"WithinLayer(term, l) lifts any ERGM.jl term into layer l, reusing the base term's validated compute/change_stat on that layer's network — the analogue of ergm.multi's L(~term, ~A). LayerEdges(l), LayerMutual(l), LayerTriangle(l) are convenience forms of the same idea with pooling support. A within-layer term that expands into several statistics in ERGM.jl (a multi-level NodeFactor, a Degree(0:2)) expands here too: one coefficient per statistic, θ of simulate_multi_ergm has one entry per statistic, and an offset on the whole term is refused (pin each level with its own single-statistic term).","title":"Within-layer terms"},{"category":"section","location":"guide/terms/#Cross-layer-terms","page":"Terms","text":"InterlayerDependence(l1, l2): the number of dyads with an edge in both layers (entrainment). Positive coefficients mean a tie in one layer predicts the same tie in the other.\nMultiplexMutual(l1, l2): ordered dyads with i→j in l1 and j→i in l2 (cross-layer exchange/reciprocity). Directed only — on an undirected MultilayerNetwork{false} it is refused with ERGM.jl's directed-only error rather than silently contributing 0.\n\nBoth count ordered dyads on directed layers, as ergm.multi does (the fixture below pins that on a case where ordered and unordered counting differ). These two are the only cross-layer forms implemented; see Not implemented for the rest of ergm.multi's layer logic.","title":"Cross-layer terms"},{"category":"section","location":"guide/terms/#Validation","page":"Terms","text":"Every term is checked against the data when the MultiERGMModel is built — which both ergm_multi and simulate_multi_ergm do first — and a problem is an ArgumentError that names the fix, before any fitting or sampling:\n\nLayer indices. Every layer a term refers to (LayerEdges(3), InterlayerDependence(1, 3), WithinLayer(t, 3)) must exist; the message quotes layer_names(m) so the index can be corrected (\"term 'L.edges.3' refers to layer 3, but the network has 2 layers (:friendship, :advice); layer indices must lie in 1:2\").\nWhat WithinLayer(t, l) checks against layer l — ERGM.jl's own formula validator, so the errors are the ones ERGM.jl and R raise: a vertex attribute the layer lacks (WithinLayer(NodeMatch(:welth), 1) → \"refers to vertex attribute :welth, which does not exist on the network\"), an attribute set on only some vertices (statnet refuses NA: \"needs vertex attribute :grp on every vertex, but 1 of 4 vertices have no value\"), an EdgeCov whose matrix is not n × n, and the direction requirement below. Attribute terms are then materialized into dense snapshots, so a later change to the attribute is not seen by a model already built.\nDirected-only and undirected-only terms. As in R ergm and ergm.multi, Kstar, GWDegree and Degree are undirected-only and Mutual, OStar, IStar, GWODegree, GWIDegree directed-only (GWESP/GWDSP work on both, with the OTP label on directed layers); the multilayer LayerMutual and MultiplexMutual are directed-only. On a layer of the wrong kind the model constructor, compute and change_stat_layer all throw ERGM.jl's sentence (\"term 'kstar2' is only defined for undirected networks, but the network is directed. Use OStar(2) / IStar(2) instead\"; \"term 'L.mutual.all' is only defined for directed networks, but the network is undirected\") — never a silent 0.0.\nA bare ERGM.jl term. Mutual() where WithinLayer(Mutual(), l) was meant — the ergm.multi migrant's L(~mutual, ~A) slip — has no multilayer change statistic and is refused by the model constructor (\"term 'mutual' (Mutual) has no multilayer change statistic (change_stat_layer): it is a single-network ERGM.jl term or a multilevel descriptive, not a multilayer term. In a multilayer model lift an ERGM.jl term into a layer with WithinLayer(Mutual(...), l) (the analogue of ergm.multi's L(~mutual, ~A)), or use the pooled/per-layer forms …\"). It used to construct and fail at fit time with a MethodError. The multilevel descriptives (Nestedness, …) are refused by the same check.\nLayers are selected by index. LayerEdges(:advice), WithinLayer(Triangle(), :advice), InterlayerDependence(:friendship, :advice) and MultiplexMutual(:friendship, 2) throw an ArgumentError at the term's construction with the index to use (\"LayerEdges selects layers by index, not by name (got :advice): use LayerEdges(k) with k = findfirst(==(:advice), layernames(m)) …\") — the migration note the CHANGELOG records, at the point where it is needed. Names are accepted by `addlayeredge!andlayernetwork`; terms are built before they meet a network, so they carry indices.\nSelf-loops and two-mode layers. A layer that contains a self-loop is refused by MultiERGMModel (a loops=true layer with none is accepted), and a two-mode (bipartite) layer by add_layer!; both sentences are in Model Estimation under Validation before fitting.\nLabels. The coefficient of a within-layer term is L<l>. followed by ERGM.jl's direction-aware label, resolved against the layer: WithinLayer(GWESP(0.5), 1) is L1.gwesp.OTP.fixed.0.5 on a directed layer and L1.gwesp.fixed.0.5 on an undirected one, exactly as ERGM.jl and R label them; the multilayer terms are L.edges.<sel>, L.mutual.<sel>, L.triangle.<sel>, duplex.<l1>.<l2> and duplex.mutual.<l1>.<l2>. show, coeftable and gof all use these.\n\nusing ERGMMulti, ERGM\nm = MultilayerNetwork(4; directed = true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\ntry\n    MultiERGMModel([WithinLayer(Kstar(2), 1)], m)\ncatch e\n    println(e.msg)      # ... only defined for undirected networks ... Use `OStar(2)` / `IStar(2)` ...\nend\nMultiERGMModel([WithinLayer(GWESP(0.5), 1)], m).terms.names   # [\"L1.gwesp.OTP.fixed.0.5\"]","title":"Validation"},{"category":"section","location":"guide/terms/#Correspondence-with-ergm.multi","page":"Terms","text":"Each row names the R term a statistic reproduces and the golden fixture that pins it (both are generated by checked-in scripts under test/fixtures/r/ from real ergm.multi 0.3.0 output, with a [provenance] block; the layers are Layer(list(A = net1, B = net2))). \"Statistic\" means summary() agrees exactly (tolerance 1e-10); \"MPLE\" means the term also enters a fit whose coefficients and standard errors are held to R's own design refit (1e-6 / 1e-4, 20x the measured glm slack). Directedness matters: twolayer_ergm_multi and sections (i)–(iii) of twolayer_layer_terms use two directed 20-actor layers (\"directed\" below); section (iv) of twolayer_layer_terms pins the undirected branch — unordered within-layer dyads (nobs = L·n(n−1)/2, ergmMPLE's weights summing to 132), unordered co-occurrence for L(~edges, ~A&B), and the undirected triangle/gwesp/kstar/gwdegree forms — on two undirected 12-actor layers drawn from the same seed (\"undirected\" below). A term with no \"undirected\" entry is directed-only (LayerMutual, MultiplexMutual, OStar, TwoPath) or is pinned on directed layers only.\n\nERGMMulti.jl ergm.multi Pinned by\nLayerEdges(1) L(~edges, ~A) twolayer_layer_terms (directed: statistic, MPLE; undirected: statistic, MPLE); twolayer_ergm_multi (N(~edges, ~factor(.NetworkID) - 1), exact MLE, with and without offsets)\nLayerEdges() / LayerEdges([1, 2]) edges on the combined Layer() network (one coefficient for L(~edges, ~A) + L(~edges, ~B)) the sum of the per-layer statistics above; logit(density) analytic test\nLayerMutual(1) L(~mutual, ~A) twolayer_layer_terms (directed: statistic, MPLE)\nLayerTriangle(1) L(~triangle, ~A) twolayer_layer_terms (directed: statistic; undirected: statistic, MPLE)\nWithinLayer(GWESP(0.5), 1) L(~gwesp(0.5, fixed = TRUE), ~A) (label gwesp.OTP.fixed.0.5 on directed layers, gwesp.fixed.0.5 on undirected) twolayer_layer_terms (directed: statistic; undirected: statistic)\nWithinLayer(OStar(2), 2) L(~ostar(2), ~B) twolayer_layer_terms (directed: statistic)\nWithinLayer(Kstar(2), 2) L(~kstar(2), ~B) twolayer_layer_terms (undirected: statistic)\nWithinLayer(GWDegree(0.3), 2) L(~gwdegree(0.3, fixed = TRUE), ~B) twolayer_layer_terms (undirected: statistic)\nWithinLayer(NodeMatch(:grp), 2) L(~nodematch(\"grp\"), ~B) twolayer_layer_terms (directed: statistic; undirected: statistic); twolayer_ergm_multi (per-layer nodematch in the exact MLE, and as offset.coef)\nWithinLayer(TwoPath(), 1) L(~twopath, ~A) twolayer_layer_terms (directed: statistic)\nWithinLayer(term, l), any other ERGM.jl term L(~term, ~A) ERGM.jl's own fixtures for term (the layer's network is passed through unchanged)\nInterlayerDependence(1, 2) L(~edges, ~A&B) (ordered dyads on directed layers, unordered on undirected) twolayer_layer_terms (directed: statistic, MPLE; undirected: statistic, MPLE)\nMultiplexMutual(1, 2) mutualL(Ls = list(~A, ~B)) twolayer_layer_terms (directed: statistic)\noffsets = Dict(k => c) offset(...) with offset.coef = c twolayer_ergm_multi (offset fit; and the fit with the term dropped, which the offset fit must not reproduce)\n\nCrossNetEdges, Nestedness, LevelHomophily and CrossLevelEdge are descriptive statistics with no ergm.multi counterpart and no fixture; they never enter a fit.","title":"Correspondence with ergm.multi"},{"category":"section","location":"guide/multilevel/#Multilevel-Networks","page":"Multilevel Networks","text":"MultilevelNetwork represents hierarchical designs: one network per level, membership maps (membership[l][node] = higher-level unit, keyed by each level's own node IDs), and explicit cross-level ties added with add_cross_level_edge!.\n\nThe associated statistics are descriptive (they do not enter ergm_multi):\n\nNestedness(level): the proportion of level-level edges whose endpoints share a higher-level unit;\nLevelHomophily(level): the count of such edges;\nCrossLevelEdge: the number of recorded cross-level ties.\n\nusing ERGMMulti, Networks\nusing ERGM: compute\n\npeople = network(5; directed = false)\norgs = network(2; directed = false)\nmembership = [Dict(1 => 1, 2 => 1, 3 => 2, 4 => 2, 5 => 2)]\nml = MultilevelNetwork([people, orgs], membership)\n\ncompute(Nestedness(1), ml)\nadd_cross_level_edge!(ml, 1, 3, 2, 1)\ncompute(CrossLevelEdge(), ml)","title":"Multilevel Networks"},{"category":"section","location":"guide/structures/#Multi-Network-Structures","page":"Multi-Network Structures","text":"","title":"Multi-Network Structures"},{"category":"section","location":"guide/structures/#MultilayerNetwork","page":"Multi-Network Structures","text":"MultilayerNetwork{D} holds L same-sized networks over one actor set, all directed (D = true) or all undirected (D = false) — the directedness is a type parameter, read with is_directed(m), and add_layer! refuses a layer of the other kind. Access layers by index or name with layer_network; add ties with add_layer_edge!. Data entry is loud: an actor id outside 1:n or a self-loop on a layer built without loops=true is an ArgumentError naming the ids and the range (never a silently dropped tie), and an unknown layer name or an out-of-range layer index is an ArgumentError naming the layers the network has.","title":"MultilayerNetwork"},{"category":"section","location":"guide/structures/#The-combined-network","page":"Multi-Network Structures","text":"combine_networks produces the ergm.multi-style block-diagonal Network: n × L vertices where vertex (l−1)·n + a is actor a's copy in layer l, carrying :layer and :actor vertex attributes, with every layer's edges inside its own block and no cross-block edges. split_by_layer inverts the construction (and rejects cross-block edges).\n\nBoth are conversions in the sense of Networks.jl's conversion contract: preserve what the target can represent, report what it cannot. A Network can carry a missing-dyad mask, so the mask is preserved — a masked (unobserved) dyad of layer l is masked at its block position of the combined network and comes back onto its layer through split_by_layer; a masked cross-block dyad is refused like a cross-block edge. What the block-diagonal network cannot hold — the layers' own vertex, edge and network attributes (only :layer/:actor are carried) — is dropped and named: pass report=true to get the Networks.ConversionReport alongside the result.\n\nusing ERGMMulti, Networks\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2)\nset_missing_dyad!(layer_network(m, :advice), 2, 3)       # unobserved in layer 2\nset_vertex_attribute!(layer_network(m, :friendship), :grp,\n                      Dict(1 => \"A\", 2 => \"A\", 3 => \"B\", 4 => \"B\"))\nc, rep = combine_networks(m; report=true)\nis_missing_dyad(c, 6, 7)          # true — actor 2→3 in block 2\nn_missing_dyads(c)                # 1\ndropped_fields(rep)               # [:grp]\nm2 = split_by_layer(c, 4, 2; names=[:friendship, :advice])\nis_missing_dyad(layer_network(m2, :advice), 2, 3)        # true — round trip\n\nas_multilayer stores the layer networks as given, so their attributes and masks survive inside the MultilayerNetwork; its report (as_multilayer(nets, names; report=true)) is lossless. None of the three adapters reads a masked dyad's face value (supports_missing is true for each); the guard against analysing a masked layer sits on the estimator, which refuses it.\n\nThe estimation and simulation routines work directly on the layered representation, whose within-layer dyads are exactly the free dyads of the block-diagonal network.","title":"The combined network"},{"category":"section","location":"guide/structures/#MultiNetwork","page":"Multi-Network Structures","text":"MultiNetwork is a container for independent networks of possibly different sizes, with the descriptive CrossNetEdges statistic.","title":"MultiNetwork"},{"category":"section","location":"api/estimation/#Estimation-and-Simulation","page":"Estimation","text":"fit_ergm_multi is the ecosystem's harmonised fit_<model> name and ergm_multi the R ergm.multi name; they are one const function. The pre-0.2 fit_multi_ergm is a deprecated wrapper.","title":"Estimation and Simulation"},{"category":"section","location":"api/estimation/#StatsAPI-surface","page":"Estimation","text":"Every verb of the ecosystem's StatsAPI surface is implemented on MultiERGMResult: coef, stderror, vcov, confint, loglikelihood, nobs, dof, aic, bic and coeftable (pinned by Networks.check_statsapi(fit; strict=true) in the test suite).\n\nEvery accessor reads the fit as it was actually made: an offset term's standard error is NaN, a boundary statistic's coefficient is ∓Inf with standard error 0, dof counts only finite non-offset coefficients, and loglikelihood is a pseudo-log-likelihood unless is_exact(fit).","title":"StatsAPI surface"},{"category":"section","location":"api/estimation/#Goodness-of-Fit","page":"Estimation","text":"ERGMMulti extends the ecosystem's single gof generic (defined in Networks.jl) with a method for a multi-network fit.","title":"Goodness of Fit"},{"category":"function","location":"api/estimation/#ERGMMulti.fit_ergm_multi","page":"Estimation","text":"fit_ergm_multi\n\nThe harmonised fit_<model> name of the multilayer ERGM fitter — a const alias of ergm_multi (fit_ergm_multi === ergm_multi), which is the R ergm.multi name. Both are exported; use whichever reads better.\n\nExample\n\nusing ERGMMulti\nfit_ergm_multi === ergm_multi   # true\n\n\n\n\n\n","title":"ERGMMulti.fit_ergm_multi"},{"category":"function","location":"api/estimation/#ERGMMulti.ergm_multi","page":"Estimation","text":"ergm_multi(m::MultilayerNetwork, terms; offsets=Dict{Int,Float64}(),\n           maxiter=100, tol=1e-8, se=:hessian, n_boot=100,\n           boot_burnin=nothing, boot_interval=nothing,\n           rng=Random.default_rng(), threaded=true) -> MultiERGMResult\nergm_multi(model::MultiERGMModel; kwargs...) -> MultiERGMResult\nfit_ergm_multi(...)                            # the same function\n\nFit a multilayer ERGM by maximum pseudo-likelihood over the within-layer dyads (the dyad universe of ergm.multi's block-diagonal construction): each dyad's conditional edge probability is logistic in θ'Δg, with Δg the add-direction change statistics of the layer-aware terms. ergm_multi is the R (ergm.multi) name and fit_ergm_multi the ecosystem's harmonised fit_<model> name; they are one const function (fit_ergm_multi === ergm_multi).\n\nThe terms are validated against the data when the MultiERGMModel is built: an out-of-range layer index, a within-layer term whose attribute the layer lacks, an undirected-only term on a directed layer (or the reverse), and a masked (missing) dyad in any layer all throw an ArgumentError before any fitting. An unconverged Newton iteration is loud: a warning is emitted, fit.converged is false, show prints the caveat and approximations(fit) records it. A statistic at the boundary of its attainable range (no co-occurring tie under InterlayerDependence, no mutual dyad under LayerMutual, a perfectly separated NodeMatch, ...) has no finite MPLE: as R ergm's default drop=TRUE, its coefficient is fixed at ∓Inf with standard error 0 (R's warning is printed), the other coefficients are estimated on the dyads it does not touch, is_exact(fit) is false, and show/approximations say so. Perfect separation by a combination of statistics (no single column at its boundary, yet the ties are perfectly predicted — R's \"The MPLE does not exist!\") is detected by ERGM.jl's asymptote test, with or without offsets: R's sentence is printed, the fit is returned with converged == false and separated == true, and show/approximations carry the verdict.\n\noffsets maps term indices to fixed coefficients (per-layer offsets, e.g. Dict(1 => -log(n)) for a size adjustment); the remaining coefficients are estimated with the offset contribution absorbed into the linear predictor.\n\nStandard errors\n\nse=:hessian (default) — the inverse negative pseudo-Hessian. Caution: with any dyad-dependent term (LayerMutual, LayerTriangle, InterlayerDependence, MultiplexMutual, ...) the pseudo-likelihood multiplies the (layer, i, j) conditionals as if independent, so these standard errors are expected to be anticonservative. With only dyad-independent terms the pseudo-likelihood is the likelihood and they are correct.\nse=:bootstrap — parametric bootstrap: simulate n_boot multilayer networks from the fitted model at θ̂ (offsets included) with simulate_multi_ergm, refit ergm_multi on each with the same offsets, and report the empirical covariance of the refits. The point estimates are unchanged; only the covariance is replaced. Offset rows stay NaN — a fixed coefficient carries no uncertainty. A replicate whose refit does not converge is excluded (warned once; fit.boot_replicates keeps it as a NaN row). This is the same option, with the same keywords and the same semantics, as ERGM.mple's, and it runs on the ONE shared Networks.bootstrap_cov loop. It is refused (ArgumentError) when a coefficient is fixed at ∓Inf by a boundary statistic: a network cannot be simulated at an infinite coefficient.\n\nKeyword Arguments\n\noffsets::Dict{Int,Float64}: fixed coefficients by term index\nmaxiter::Int=100, tol::Float64=1e-8: Newton iteration controls\nse::Symbol=:hessian: :hessian or :bootstrap (above; anything else is refused by the shared Networks.check_se)\nn_boot::Int=100: number of bootstrap replicates (se=:bootstrap only)\nboot_burnin, boot_interval: MCMC controls for the bootstrap simulations; nothing (default) resolves to the dyad-scaled rule shared with every ERGM-family sampler (ERGM._mcmc_defaults: 20 × n_dyads and max(100, n_dyads ÷ 10) over the within-layer dyads)\nrng::AbstractRNG=Random.default_rng(): source of the bootstrap randomness — a fixed rng reproduces the standard errors exactly\nthreaded::Bool=true: run the bootstrap refits on all threads. The replicates are simulated from rng before any refit and each refit is deterministic, so the standard errors are identical whatever the thread count (pinned by a test)\n\nExample\n\nusing ERGMMulti, ERGM, Networks, Random\nrng = Xoshiro(1)\nm = MultilayerNetwork(12; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nfor i in 1:12, j in 1:12\n    i == j && continue\n    rand(rng) < 0.2 && add_layer_edge!(m, :friendship, i, j)\n    rand(rng) < 0.2 && add_layer_edge!(m, :advice, i, j)\nend\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2), InterlayerDependence(1, 2)])\nfit.converged                    # true\ncoeftable(fit).names             # [\"L.edges.1\", \"L.edges.2\", \"duplex.1.2\"]\nsize(confint(fit))               # (3, 2)\n\n\n\n\n\n","title":"ERGMMulti.ergm_multi"},{"category":"function","location":"api/estimation/#ERGMMulti.fit_multi_ergm","page":"Estimation","text":"fit_multi_ergm(args...; kwargs...)\n\nDeprecated pre-0.2 alias of ergm_multi: emits a deprecation warning and forwards every argument to ergm_multi. Use fit_ergm_multi (the ecosystem's fit_<model> name) or ergm_multi (the R name).\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :b, 2, 3)\nfit = fit_multi_ergm(m, [LayerEdges()])      # warns: deprecated, use fit_ergm_multi\ncoef(fit) == coef(fit_ergm_multi(m, [LayerEdges()]))   # true\n\n\n\n\n\n","title":"ERGMMulti.fit_multi_ergm"},{"category":"function","location":"api/estimation/#ERGMMulti.simulate_multi_ergm","page":"Estimation","text":"simulate_multi_ergm(m::MultilayerNetwork, terms, θ; n_sim=1, burnin=nothing,\n                    interval=nothing, rng=Random.default_rng())\n    -> Vector{MultilayerNetwork}\nsimulate_multi_ergm(model::MultiERGMModel, θ; kwargs...)\n\nSimulate multilayer networks from P(y) ∝ exp(θ'g(y)) with a Metropolis sampler restricted to within-layer dyads (matching the dyad universe of the block-diagonal model): propose toggling a random (layer, i, j), accept an addition with min(1, exp(θ'Δg)) and a removal with min(1, exp(−θ'Δg)). The chain is the ONE shared Metropolis kernel of the ERGM family, ERGM.mh_toggle!, with (layer, i, j) moves; the rng is drawn proposal-first, then acceptance, so seeded runs are reproducible.\n\nThe (m, terms, θ) form builds a MultiERGMModel first, so the terms are validated against the layers (an out-of-range layer, a missing attribute, an undirected-only term on a directed layer, ... throw before the chain starts). θ has one entry per statistic of the model (model.terms.names), which equals one per term unless a WithinLayer term expands (a multi-level NodeFactor, a Degree(0:2)).\n\nburnin/interval default (nothing) to the dyad-scaled rule shared with every ERGM-family sampler (ERGM._mcmc_defaults): 20 × n_dyads and max(100, n_dyads ÷ 10) over the within-layer dyads.\n\nA layer with a masked (missing) dyad is refused with an ArgumentError naming the layer (Networks.missing_policies(simulate_multi_ergm) == (:error,)): the chain would otherwise toggle the unobserved dyad at its face value. Every coefficient must be finite: a ∓Inf (a boundary statistic's R-drop value, coef(fit) of such a fit) or NaN entry is refused with an ArgumentError naming it (\"simulatemultiergm: every coefficient must be finite (got duplex.1.2 = -Inf) …\") — the chain would otherwise reject every proposal (θ'Δ is NaN) and return copies of the input. Remove the dropped term, as R's drop=TRUE does, and simulate from that model.\n\nExample\n\nusing ERGMMulti, ERGM, Random\nm = MultilayerNetwork(8; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\ndraws = simulate_multi_ergm(m, [LayerEdges(), InterlayerDependence(1, 2)],\n                            [-1.5, 2.0]; n_sim=10, rng=Xoshiro(1))\nlength(draws)                         # 10\ndraws[1] isa MultilayerNetwork{true}  # true\n\n\n\n\n\n","title":"ERGMMulti.simulate_multi_ergm"},{"category":"method","location":"api/estimation/#StatsAPI.coef-Tuple{MultiERGMResult}","page":"Estimation","text":"coef(r::MultiERGMResult) -> Vector{Float64}\n\nThe fitted coefficients, one per statistic of r.model.terms (labelled by r.model.terms.names). An offset term reports its fixed value; a statistic at the boundary of its attainable range reports ∓Inf (R's drop).\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :a, 2, 3); add_layer_edge!(m, :b, 1, 3)\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\ncoef(fit) ≈ [log(2 / 10), log(1 / 11)]    # true — logit(density) per layer\n\n\n\n\n\n","title":"StatsAPI.coef"},{"category":"method","location":"api/estimation/#StatsAPI.stderror-Tuple{MultiERGMResult}","page":"Estimation","text":"stderror(r::MultiERGMResult) -> Vector{Float64}\n\nThe standard errors the fit reports — inverse pseudo-Hessian or parametric bootstrap, whichever se_method(r) says; NaN for an offset term, 0.0 for a coefficient fixed at ∓Inf.\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :a, 2, 3); add_layer_edge!(m, :b, 1, 3)\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\nstderror(fit)[1] ≈ sqrt(1 / (12 * (2 / 12) * (10 / 12)))   # true — binomial information\nfit_off = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)]; offsets=Dict(2 => -2.0))\nisnan(stderror(fit_off)[2])                                 # true — fixed, no uncertainty\n\n\n\n\n\n","title":"StatsAPI.stderror"},{"category":"method","location":"api/estimation/#StatsAPI.vcov-Tuple{MultiERGMResult}","page":"Estimation","text":"vcov(r::MultiERGMResult) -> Matrix{Float64}\n\nThe covariance matrix of the coefficients (p × p), of which stderror(r) is the square root of the diagonal; offset rows and columns are NaN.\n\nExample\n\nusing ERGMMulti, LinearAlgebra\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :a, 2, 3); add_layer_edge!(m, :b, 1, 3)\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\nsize(vcov(fit))                              # (2, 2)\nsqrt.(diag(vcov(fit))) ≈ stderror(fit)       # true\nvcov(fit)[1, 2] ≈ 0.0                        # true — the layers' MPLEs are independent\n\n\n\n\n\n","title":"StatsAPI.vcov"},{"category":"method","location":"api/estimation/#StatsAPI.confint-Tuple{MultiERGMResult}","page":"Estimation","text":"confint(r::MultiERGMResult; level=0.95) -> Matrix{Float64}\n\nNormal-theory (Wald) confidence limits θ̂ ± z_{(1+level)/2} · se, one row per coefficient with the lower limit in column 1 and the upper in column 2 (a method of StatsAPI.confint). The standard errors are the ones the fit reports (se_method(r): inverse pseudo-Hessian or parametric bootstrap), so for a dyad-dependent model under se=:hessian the intervals inherit the anticonservative pseudo-likelihood SEs. Offset rows are NaN: a fixed coefficient has no interval.\n\nExample\n\nusing ERGMMulti, ERGM, Networks, Random\nrng = Xoshiro(3)\nm = MultilayerNetwork(10; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nfor l in 1:2, i in 1:10, j in 1:10\n    i != j && rand(rng) < 0.3 && add_layer_edge!(m, l, i, j)\nend\nfit = ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\nci = confint(fit)                          # 2×2\nall(ci[:, 1] .< coef(fit) .< ci[:, 2])     # true\n\n\n\n\n\n","title":"StatsAPI.confint"},{"category":"method","location":"api/estimation/#StatsAPI.coeftable-Tuple{MultiERGMResult}","page":"Estimation","text":"coeftable(r::MultiERGMResult) -> Networks.CoefficientTable\n\nThe R-style coefficient table (Estimate, Std.Error, z value, Pr(>|z|)) as an inspectable Networks.CoefficientTable — exactly the table show(r) prints, built from the same vectors (a method of StatsAPI.coeftable). Rows are labelled with the model's direction-aware statistic names (r.model.terms.names), offset rows tagged \" (offset)\" with NaN standard error, z and p; the z → p map is the shared Networks.z_pvalues.\n\nExample\n\nusing ERGMMulti, ERGM, Networks, Random\nrng = Xoshiro(3)\nm = MultilayerNetwork(10; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nfor l in 1:2, i in 1:10, j in 1:10\n    i != j && rand(rng) < 0.3 && add_layer_edge!(m, l, i, j)\nend\nfit = ergm_multi(m, [LayerEdges(1), LayerEdges(2)]; offsets=Dict(2 => -1.0))\ntbl = coeftable(fit)\ntbl.names                                   # [\"L.edges.1\", \"L.edges.2 (offset)\"]\ntbl[\"L.edges.1\"].estimate == coef(fit)[1]   # true\nisnan(tbl[2].p_value)                       # true\n\n\n\n\n\n","title":"StatsAPI.coeftable"},{"category":"method","location":"api/estimation/#StatsAPI.loglikelihood-Tuple{MultiERGMResult}","page":"Estimation","text":"loglikelihood(r::MultiERGMResult) -> Float64\n\nThe maximized pseudo-log-likelihood over the within-layer dyads — the log-likelihood itself only when every term is dyad-independent (is_exact(r)).\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :a, 2, 3); add_layer_edge!(m, :b, 1, 3)\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\nll = 2 * log(2 / 12) + 10 * log(10 / 12) + log(1 / 12) + 11 * log(11 / 12)\nloglikelihood(fit) ≈ ll                      # true — two independent binomials\n\n\n\n\n\n","title":"StatsAPI.loglikelihood"},{"category":"method","location":"api/estimation/#StatsAPI.nobs-Tuple{MultiERGMResult}","page":"Estimation","text":"nobs(r::MultiERGMResult) -> Int\n\nThe number of within-layer dyads — the rows of the pseudo-likelihood: L · n · (n − 1) for directed layers, L · n · (n − 1) / 2 for undirected.\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2)\nnobs(fit_ergm_multi(m, [LayerEdges()]))      # 24 — 2 layers × 12 ordered dyads\n\n\n\n\n\n","title":"StatsAPI.nobs"},{"category":"method","location":"api/estimation/#StatsAPI.dof-Tuple{MultiERGMResult}","page":"Estimation","text":"dof(r::MultiERGMResult) -> Int\n\nThe number of estimated parameters: the free (non-offset) coefficients that are finite — a coefficient fixed at ∓Inf by a boundary statistic is not an estimated parameter, and neither is an offset.\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :b, 1, 3)\ndof(fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)]))                          # 2\ndof(fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)]; offsets=Dict(1 => -2.0)))  # 1\n\n\n\n\n\n","title":"StatsAPI.dof"},{"category":"method","location":"api/estimation/#StatsAPI.aic-Tuple{MultiERGMResult}","page":"Estimation","text":"aic(r::MultiERGMResult) -> Float64\n\n-2 · loglikelihood(r) + 2 · dof(r) (pseudo-likelihood based whenever the model has a dyad-dependent term).\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :a, 2, 3); add_layer_edge!(m, :b, 1, 3)\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\naic(fit) ≈ -2 * loglikelihood(fit) + 2 * dof(fit)    # true\n\n\n\n\n\n","title":"StatsAPI.aic"},{"category":"method","location":"api/estimation/#StatsAPI.bic-Tuple{MultiERGMResult}","page":"Estimation","text":"bic(r::MultiERGMResult) -> Float64\n\n-2 · loglikelihood(r) + dof(r) · log(n), where n is the number of within-layer dyads the coefficients were estimated on (nobs(r), unless a boundary statistic was dropped — then the dyads it does not touch).\n\nExample\n\nusing ERGMMulti\nm = MultilayerNetwork(4; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nadd_layer_edge!(m, :a, 1, 2); add_layer_edge!(m, :a, 2, 3); add_layer_edge!(m, :b, 1, 3)\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\nbic(fit) ≈ -2 * loglikelihood(fit) + dof(fit) * log(nobs(fit))    # true\n\n\n\n\n\n","title":"StatsAPI.bic"},{"category":"method","location":"api/estimation/#Networks.gof-Tuple{MultiERGMResult}","page":"Estimation","text":"gof(result::MultiERGMResult; n_sim=100, burnin=nothing, interval=nothing,\n    rng=Random.default_rng()) -> GOFResult\n\nGoodness-of-fit assessment for a fitted multilayer ERGM: simulate n_sim multilayer networks at the fitted (and offset) coefficients with simulate_multi_ergm and compare the observed data against the simulated distributions on two panels:\n\n\"model statistics\" — the fitted terms' statistics (one level per statistic, labelled as in coeftable(result));\n\"layer edges\" — the edge count of each layer (one level per layer).\n\nburnin/interval default to the dyad-scaled rule of every ERGM-family sampler (ERGM._mcmc_defaults). Extends Networks.jl's shared gof generic and returns the shared Networks.GOFResult container; per-level p-values are two-sided Monte-Carlo p-values computed with the (1 + k)/(N + 1) estimator (never exactly zero). The fitted network is never masked (the estimator refused it), so the simulation needs no missing= policy.\n\nA fit with a coefficient fixed at ∓Inf (a statistic at the boundary of its attainable range, R's drop) is refused with an ArgumentError naming the term (\"gof: every coefficient must be finite (got duplex.1.2 = -Inf) …\"): nothing can be simulated at an infinite coefficient, and the frozen chain would otherwise report p = 1 for every statistic. Drop the term and refit, as R's drop=TRUE does, then assess that model.\n\nExample\n\nusing ERGMMulti, ERGM, Networks, Random\nrng = Xoshiro(4)\nm = MultilayerNetwork(10; directed=true)\nadd_layer!(m, :a); add_layer!(m, :b)\nfor l in 1:2, i in 1:10, j in 1:10\n    i != j && rand(rng) < 0.3 && add_layer_edge!(m, l, i, j)\nend\nfit = fit_ergm_multi(m, [LayerEdges(1), LayerEdges(2)])\ng = gof(fit; n_sim=20, burnin=2000, interval=50, rng=Xoshiro(5))\ng isa GOFResult                            # true\n[panel.name for panel in g.statistics]     # [\"model statistics\", \"layer edges\"]\ng.statistics[2].labels                     # [\"a\", \"b\"] — one level per layer\nall(0 .< g.statistics[1].p_values .<= 1)   # true — (1+k)/(N+1), never 0\n\n\n\n\n\n","title":"Networks.gof"},{"category":"section","location":"guide/estimation/#Model-Estimation","page":"Model Estimation","text":"ergm_multi (= fit_ergm_multi, one const function: the R name and the ecosystem's fit_<model> name) fits by maximum pseudo-likelihood over the within-layer dyads: each (layer, i, j) contributes a logistic term in θ'Δg with Δg from change_stat_layer. The pseudo-likelihood is maximized with the shared Networks.newton_fit optimizer (Newton-Raphson with step-halving) on the shared, allocation-free Networks.logistic_derivatives kernel — the same code ERGM.jl's MPLE, TERGM.jl's CMPLE and ERGMRank.jl's swap MPLE run on. Standard errors come from the inverse observed information of the pseudo-likelihood, or from a parametric bootstrap (se=:bootstrap). There is no MCMLE — see Not implemented.\n\nusing ERGMMulti, Networks, Random\n\nrng = Xoshiro(1)\nm = MultilayerNetwork(20; directed = true)\nadd_layer!(m, :friendship)\nadd_layer!(m, :advice)\nfor i in 1:20, j in 1:20\n    i == j && continue\n    rand(rng) < 0.1 && add_layer_edge!(m, :friendship, i, j)\n    rand(rng) < 0.1 && add_layer_edge!(m, :advice, i, j)\nend\nterms = [LayerEdges(), InterlayerDependence(1, 2)]\n\nresult = fit_ergm_multi(m, terms)\ncoef(result)\nstderror(result)\nloglikelihood(result)      # maximized pseudo-log-likelihood\naic(result), bic(result)\nnobs(result), dof(result)  # 760 within-layer dyads, 2 estimated coefficients\nconfint(result)            # normal-theory limits\ncoeftable(result)          # the R-style table `show` prints\nis_exact(result)           # false — InterlayerDependence is dyad-dependent\napproximations(result)     # says so, in words\n\nmaxiter (default 100) and tol (default 1e-8) control the Newton iteration; se, n_boot, boot_burnin, boot_interval, rng and threaded control the bootstrap (below). The keyword vocabulary is the ERGM family's (maxiter, n_sim, rng), so a call written for fit_ergm reads the same here.","title":"Model Estimation"},{"category":"section","location":"guide/estimation/#Validation-before-fitting","page":"Model Estimation","text":"The MultiERGMModel constructor (which both ergm_multi and simulate_multi_ergm go through) checks the formula against the data and throws an ArgumentError naming the fix — before any fitting:\n\na layer index the network does not have (LayerEdges(3) on two layers) names the layer and layer_names(m);\na WithinLayer(term, l) is validated against layer l with ERGM.jl's own validator: a vertex attribute the layer lacks (WithinLayer(NodeMatch(:welth), 1)), an attribute set on only some vertices (statnet refuses NA), an EdgeCov of the wrong size, an undirected-only term (Kstar, GWDegree, Degree) on a directed layer, a directed-only one (Mutual, OStar, ...) on an undirected layer;\nLayerMutual and MultiplexMutual are directed-only;\na bare ERGM.jl term (Mutual() where WithinLayer(Mutual(), l) was meant — ergm.multi's L(~mutual, ~A)) or a multilevel descriptive (Nestedness(1)) is refused with the WithinLayer fix spelled out (\"term 'mutual' (Mutual) has no multilayer change statistic … lift an ERGM.jl term into a layer with WithinLayer(Mutual(...), l)\"), not left to die inside the design builder with a MethodError;\na layer that contains a self-loop is refused, as ERGM.ERGMModel refuses it (\"layer :b (layer 2) contains 1 self-loop (at vertex 1). ERGMMulti models the off-diagonal dyads of every layer only … R ergm warns \\\"This network contains loops\\\" here\"): the statistics would count the loop while the within-layer design, nobs, the sampler and gof never touch the diagonal. A layer built with loops=true that holds no loop is accepted;\noffsets must index terms, be finite, leave at least one coefficient free, and not target a term that expands into several statistics;\nthe network must have at least one layer and at least two actors: a MultilayerNetwork(1) (or (0)) has no within-layer dyad, and the constructor says so (\"the network has no within-layer dyads (n = 1 actor, 1 layer); a multilayer ERGM needs at least two actors — nothing to estimate or simulate\") instead of running Newton on an empty design.\n\nThree checks happen even earlier. Data entry is loud: add_layer_edge! refuses an actor id outside 1:n (\"addlayeredge!: actor ids must lie in 1:10 (got (0, 2)); the multilayer network has 10 actors. Ids are 1-based — a 0 usually means 0-based data — and an edge is never dropped silently.\") and a self-loop on a layer built without loops=true, and layer_network with an out-of-range index names the layers the network has (\"layer index 5 out of range: the network has 2 layers (:friendship, :advice); layer indices must lie in 1:2\") — a mistyped id can no longer leave you with a silently smaller network. A two-mode (bipartite) layer is refused by add_layer!/as_multilayer (\"layer :a is two-mode (bipartite); ERGMMulti models one-mode layers only — the within-layer dyad universe would enumerate the impossible within-mode pairs as observed non-ties …\"): it never enters a MultilayerNetwork. And a term that selects a layer by name (LayerEdges(:advice), WithinLayer(Triangle(), :advice), InterlayerDependence(:friendship, :advice)) is an ArgumentError at the term's construction, giving the index to use (\"use LayerEdges(k) with k = findfirst(==(:advice), layer_names(m))\").\n\nThe full list, with the sentences a user reads, is in Terms under Validation.","title":"Validation before fitting"},{"category":"section","location":"guide/estimation/#Offsets","page":"Model Estimation","text":"offsets = Dict(k => c) fixes term k's coefficient at c — the ergm.multi offset mechanism used for per-layer size adjustments or theory-fixed effects. Offset terms report their fixed coefficient with NaN standard error (and a NaN confidence interval, NaN rows of vcov); the free coefficients are estimated with the offset contribution absorbed into the linear predictor, and dof excludes the offsets. Offsets index the terms as given; a within-layer term that expands into several statistics (a multi-level NodeFactor, a Degree(0:2)) cannot be offset as a whole — pin each level or degree with its own single-statistic term. The golden fixture checks the offset fit against ergm.multi's offset.coef and against the fit with the term dropped, which an offset fit must not reproduce.","title":"Offsets"},{"category":"section","location":"guide/estimation/#Boundary-statistics-and-separation","page":"Model Estimation","text":"A statistic at the boundary of its attainable range — no co-occurring tie under InterlayerDependence, no mutual dyad under LayerMutual, a NodeMatch with no within-group tie in its layer — has no finite maximum pseudo-likelihood estimate. ERGMMulti.jl applies R ergm's default drop=TRUE semantics for one-mode terms. (ergm.multi 0.3.0 does not inherit them: its layer operator does not propagate the statistic's attainable range to ergm.checkextreme.model, so for the same design R warns \"The MPLE does not exist!\" and returns a finite ≈ −17.6 with a standard error in the thousands for the boundary term — while its finite coefficients agree with ERGMMulti.jl's exactly. That R output is frozen as section (iii) of test/fixtures/twolayer_layer_terms.toml, a pinned divergence.) The warning is R's sentence:\n\nergm_multi: observed statistic(s) duplex.1.2 are at their smallest attainable values. Their coefficients will be fixed at -Inf (no finite maximum pseudo-likelihood estimate exists; R ergm's drop=TRUE; ergm.multi 0.3.0 warns \"The MPLE does not exist!\" and returns a finite value with a standard error in the thousands instead — see the estimation guide). The remaining coefficients are estimated on the dyads these statistics do not touch — the exact limit of the pseudo-likelihood — with standard error 0 and p-value 0 recorded for the fixed ones.\n\n(\"largest attainable values … fixed at +Inf\" for a statistic at its maximum.) What the result then holds:\n\ncoef(fit)[k] == -Inf (or +Inf), stderror(fit)[k] == 0.0, z ∓Inf, p-value 0 — the coeftable row is printed as such;\nthe other coefficients are the MPLE on the dyads the dropped statistic does not touch (the exact limit of the pseudo-likelihood as the coefficient goes to ∓Inf); bic uses that dyad count as its sample size and dof counts only the finite coefficients;\nfit.converged is true (the reduced problem converged), is_exact(fit) is false, and show(fit)/approximations(fit) carry the note \"duplex.1.2 fixed at -Inf (observed statistic at its smallest attainable value): no finite maximum pseudo-likelihood estimate exists …\";\nse=:bootstrap is refused on such a fit with an ArgumentError (\"se=:bootstrap is not available when a coefficient is fixed at ±Inf …\"): a network cannot be simulated at an infinite coefficient;\ngof(fit) and simulate_multi_ergm(fit.model, coef(fit)) are refused the same way (\"gof: every coefficient must be finite (got duplex.1.2 = -Inf): a multilayer network cannot be simulated at an infinite or NaN coefficient — the chain would reject every proposal and return copies of the observed network …\"). They used to run that frozen chain silently and gof reported p = 1 for every statistic. Remove the dropped term, as R's drop=TRUE does, refit, and assess that model.\n\nusing ERGMMulti, Networks\nm = MultilayerNetwork(30; directed=true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nadd_layer_edge!(m, :friendship, 1, 2)          # one tie, no co-occurrence\nfit = ergm_multi(m, [LayerEdges(), InterlayerDependence(1, 2)])  # warns\ncoef(fit)[2] == -Inf                # true\nstderror(fit)[2] == 0.0             # true\ncoef(fit)[1] ≈ log(1 / 1738)        # true: the MPLE on the 1739 untouched dyads\ndof(fit)                            # 1\nis_exact(fit)                       # false\n\nPerfect separation by a combination of statistics — no single column at its boundary, yet a linear combination of them perfectly predicts the ties — is R's \"The MPLE does not exist!\". ERGM.jl's asymptote test detects it, with or without offsets (the offset contribution is handed to the test as a column with a fixed coefficient of 1); the warning is\n\nergm_multi: the MPLE does not exist (perfect separation): the pseudo-likelihood has no finite maximum, and the returned coefficients are the point at which Newton stopped on its flat asymptote — arbitrarily large, with meaningless standard errors. R ergm warns \"The MPLE does not exist!\" for the same design. The fit is returned with converged == false; …\n\nand the fit comes back with fit.converged == false, fit.separated == true, is_exact(fit) == false, and the same verdict printed by show (under converged: false) and listed by approximations(fit) instead of the generic non-convergence sentence.","title":"Boundary statistics and separation"},{"category":"section","location":"guide/estimation/#Non-convergence","page":"Model Estimation","text":"A Newton iteration that runs out of maxiter is loud: the warning\n\nergm_multi: the Newton iteration did not converge within maxiter = 100 iterations (the maximum pseudo-likelihood estimate may not exist: a statistic at the boundary of its attainable range, or perfect separation); the result reports converged = false and its point estimates and standard errors are unreliable\n\nis emitted once, fit.converged == false, is_exact(fit) == false, show(fit) prints the caveat right under converged: false, and approximations(fit) lists it as its first entry. A rank-deficient design (two copies of the same statistic, [LayerEdges(1), LayerEdges(1)]) is reported the same way, plus Networks.jl's warning \"newton_fit: the Hessian at the solution is not negative definite; standard errors are undefined (returned as NaN)\" — stderror, vcov and confint are then NaN. An unconverged fit is never returned as a fit with a footnote: the warning, the field, the printout and the metadata all agree.","title":"Non-convergence"},{"category":"section","location":"guide/estimation/#Standard-errors","page":"Model Estimation","text":"se=:hessian (default): the inverse negative pseudo-Hessian at the optimum. With only dyad-independent terms (LayerEdges, WithinLayer(NodeMatch(...)), ...) the pseudo-likelihood is the likelihood and these are the usual maximum-likelihood standard errors (the golden fixture holds them to ~1e-11 against R). With any dyad-dependent term (LayerMutual, LayerTriangle, InterlayerDependence, MultiplexMutual, a WithinLayer(GWESP(...))) the (layer, i, j) conditionals are multiplied as if independent, so they are expected to be anticonservative: show says so, and approximations(fit) lists \"inverse-Hessian standard errors of the naive pseudo-likelihood: expected anticonservative under dependence\".\nse=:bootstrap: a parametric bootstrap on the ONE shared Networks.bootstrap_cov loop. n_boot (default 100) multilayer networks are simulated from the fitted model at θ̂ (offsets included) with simulate_multi_ergm — boot_burnin/boot_interval default to the dyad-scaled rule below, rng seeds the draws — each is refit with the same offsets, and the empirical covariance of the refitted free coefficients replaces vcov. The point estimates are unchanged; se_method(fit) reports :bootstrap; offset rows stay NaN.\nReplicate exclusion. A replicate whose refit has no finite MPLE (a simulated network with no triangle under LayerTriangle, no co-occurring tie under InterlayerDependence, a separated design) is excluded from the covariance and kept as a NaN row of fit.boot_replicates (an n_boot × p_free matrix; nothing under se=:hessian). One warning reports \"k of the n_boot bootstrap refits did not converge … and were excluded\", show/approximations(fit) carry the count, and fewer than two finite refits is an ArgumentError. The excluded replicates say something about the simulated networks, not about the observed one.\nThreads. The replicates are simulated from rng before any refit and every refit is deterministic, so threaded=false gives bit-identical standard errors to the default threaded loop — the numbers never depend on the thread count (pinned by a test that CI runs on four threads).\n\nusing ERGMMulti, Networks, Random\nrng = Xoshiro(1)\nm = MultilayerNetwork(20; directed = true)\nadd_layer!(m, :friendship); add_layer!(m, :advice)\nfor i in 1:20, j in 1:20\n    i == j && continue\n    rand(rng) < 0.15 && add_layer_edge!(m, :friendship, i, j)\n    rand(rng) < 0.15 && add_layer_edge!(m, :advice, i, j)\nend\nfit = fit_ergm_multi(m, [LayerEdges(), LayerMutual()]; se = :bootstrap,\n                     n_boot = 30, rng = Xoshiro(2))\nse_method(fit)                       # :bootstrap\nsize(fit.boot_replicates)            # (30, 2)\napproximations(fit)                  # the MPLE caveat and the bootstrap description","title":"Standard errors"},{"category":"section","location":"guide/estimation/#Missing-dyads","page":"Model Estimation","text":"Every layer must be fully observed. The MPLE enumerates every within-layer dyad as an observed row, so a masked (unobserved) dyad would enter the pseudo-likelihood at its face value; ergm_multi therefore calls require_observed on every layer and refuses, naming the layer:\n\nArgumentError: ergmmulti (layer 2) does not support missing (unobserved) dyads, but the network has 1 masked dyad. A masked dyad is unobserved, not absent, so reading its face value would silently invent data.   • call `clearmissingdyads!(net)to declare every dyad observed; or   • use a routine that implements missing-data handling     (Networks.supportsmissing(f) == true`).\n\nThere is no missing= keyword on the estimator (Networks.missing_policies(ergm_multi) == (:error,)): no policy other than refusal is offered, because none would be honest for an estimator that enumerates every dyad. simulate_multi_ergm refuses a masked layer the same way (its message starts simulate_multi_ergm (layer 2)): the chain would otherwise toggle the unobserved dyad at its face value. gof is covered transitively — a MultiERGMResult never holds a masked network. The adapters (combine_networks, split_by_layer, as_multilayer) carry the mask rather than read it, so a masked layer survives a round trip and is refused at the estimator, not silently un-masked on the way.","title":"Missing dyads"},{"category":"section","location":"guide/estimation/#Defaults","page":"Model Estimation","text":"The sampler's burnin and interval — on simulate_multi_ergm, on gof and as boot_burnin/boot_interval on the bootstrap — default to nothing, which resolves to the ERGM family's one dyad-scaled rule (ERGM._mcmc_defaults) over the within-layer dyads n_dyads = L · n · (n − 1) (directed) or L · n · (n − 1) / 2 (undirected):\n\nburnin = 20 × n_dyads,\ninterval = max(100, n_dyads ÷ 10).\n\nA 4-actor directed two-layer network (24 dyads) gets burnin = 480, interval = 100; the 20-actor fixture (760 dyads) gets burnin = 15200, interval = 100. Passing an explicit burnin/interval overrides the rule for that call. The remaining defaults are n_sim = 1 (sampler), n_sim = 100 (gof), n_boot = 100, maxiter = 100, tol = 1e-8, se = :hessian, threaded = true, and rng = Random.default_rng() — every random draw flows through rng, so a seeded call is reproducible.","title":"Defaults"},{"category":"section","location":"guide/estimation/#Checking","page":"Model Estimation","text":"An edges-only model reproduces logit(density) exactly (per layer or pooled), and simulation→estimation round trips recover coefficients — both are covered in the test suite, as are two provenanced golden fixtures against real ergm.multi 0.3.0 output: a dyad-independent fit with and without offsets (test/fixtures/twolayer_ergm_multi.toml; coefficients to ~1e-13 against R's glm at epsilon = 1e-14), and, on the same frozen layers, summary() of every term with an ergm.multi counterpart plus the MPLE of a dyad-dependent model with L(~edges, ~A&B) and L(~mutual, ~A) (test/fixtures/twolayer_layer_terms.toml; statistics exact, coefficients to ~1e-7 against R's own compressed design refit at glm epsilon 1e-14). The term-by-term correspondence is tabulated in Terms.\n\nFits whose formula contains dyad-dependent terms (classified by ERGM.is_dyad_dependent, extended to the multilayer terms, through the shared ERGM.has_dyad_dependent(model) predicate) print the pseudo-likelihood caveat, report is_exact(fit) == false, and list the approximation in approximations(fit); se=:bootstrap replaces the covariance with a parametric bootstrap.","title":"Checking"},{"category":"section","location":"guide/estimation/#Not-implemented-(known-limitations)","page":"Model Estimation","text":"The four items — no MCMLE, no covariate-driven multi-network models, layer logic limited to A&B and mutualL, descriptive-only multilevel statistics — are stated with the exact behaviour a user gets in Not implemented on the index page (and identically in the README and the CHANGELOG). The one that matters most here: there is no method=:mcmle; a dyad-dependent multilayer fit is an MPLE point estimate with the caveat above, is_exact(fit) == false, and se=:bootstrap as the remedy for the standard errors.","title":"Not implemented (known limitations)"},{"category":"section","location":"#ERGMMulti.jl","page":"Home","text":"Model several binary relations observed on the same actors, such as marriage and business ties among the same families. ERGMMulti.jl provides layer-specific and pooled effects, selected cross-layer statistics, and maximum pseudo-likelihood estimation.\n\nFirst analysis Learn the model or data Reference and detail\nAssemble and fit layers Understand multilayer data Estimation and uncertainty\n\nnote: Supported scope\nFitting accepts one-mode layers with the same actors and directedness. Estimation is MPLE; MCMC-MLE is not implemented. Multilevel and multiple-network containers have descriptive uses but are not alternate fitted model families. See Not implemented for the precise boundary.","title":"ERGMMulti.jl"},{"category":"section","location":"#Installation","page":"Home","text":"<p>Use Julia <strong>1.12 or newer</strong> and the <a href=\"/getting-started/\">shared workspace installation guide</a>. These development packages are not yet registered; the guide prepares the required sibling checkouts and a Julia environment for the examples.</p>","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"Compare the baseline tie propensities of two relations on the same 16 Florentine families:\n\nusing Networks, ERGMMulti\n\nmarriage = load_dataset(:florentine_marriage)\nbusiness = load_dataset(:florentine_business)\nlayers = as_multilayer([marriage, business], [:marriage, :business])\nfit = fit_ergm_multi(layers, [LayerEdges(1), LayerEdges(2)])\ndisplay(fit)\n\nEach coefficient is a separate layer’s baseline log-odds. This model does not test whether the two relations co-occur: that requires a cross-layer statistic such as InterlayerDependence(1, 2) and attention to the resulting pseudo-likelihood approximation.","title":"Quick Start"},{"category":"section","location":"#The-block-diagonal-mechanism","page":"Home","text":"The layers of a MultilayerNetwork combine into one block-diagonal network (combine_networks) whose vertices carry :layer and :actor attributes (a layer's missing-dyad mask is carried along and restored by split_by_layer; pass report=true for a ConversionReport of what the block-diagonal network cannot hold). The model's dyad universe is the within-layer dyads; layer-aware terms then express per-layer effects, pooled effects, and cross-layer dependence.","title":"The block-diagonal mechanism"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\n    \"getting_started.md\",\n    \"guide/structures.md\",\n    \"guide/terms.md\",\n    \"guide/estimation.md\",\n    \"guide/multilevel.md\",\n    \"api/types.md\",\n    \"api/terms.md\",\n    \"api/estimation.md\",\n]\nDepth = 2","title":"Contents"},{"category":"section","location":"#not-implemented","page":"Home","text":"These are the parts of ergm.multi that have no counterpart here. Each entry states exactly what a user gets instead; the same four items appear in the README and in the CHANGELOG.\n\nNo MCMLE. Estimation is maximum pseudo-likelihood over the within-layer dyads, for every model; there is no method=:mcmle keyword, so for a dyad-dependent multilayer model R's ergm(Layer(...) ~ ...) default estimator (MCMLE) has no counterpart. What you get: an MPLE point estimate, is_exact(fit) == false, a pseudo-likelihood caveat printed by show, approximations(fit) naming the approximation, and se = :bootstrap for model-based simulation and refitting of the covariance (the point estimates are unchanged by it).\nNo covariate-driven multi-network models. ergm.multi's Networks()-style models with per-network coefficients driven by a network-level covariate (N(~edges, ~x)) are not available; MultiNetwork is a descriptive container (CrossNetEdges only) and ergm_multi accepts a MultilayerNetwork only. What you get: the per-layer (LayerEdges(l)) and pooled (LayerEdges(), LayerEdges([1, 2])) parameterisations of a multilayer network — every layer-specific effect is its own coefficient or an offset, never a function of a covariate.\nLayer logic is limited to same-dyad conjunction and cross-layer reciprocity. InterlayerDependence(l1, l2) is L(~edges, ~A&B) and MultiplexMutual(l1, l2) is mutualL(Ls = list(~A, ~B)). twostarL, the espL/dspL/gwespL cross-layer path families, CMBL, general layer-logic formulas (~A|B, ~!A, ~A&!B, ...) and bipartite layers (b1dspL, ...) are not implemented. What you get: no such term exists to construct, and a two-mode (bipartite) network is refused by add_layer!/as_multilayer with \"layer :a is two-mode (bipartite); ERGMMulti models one-mode layers only — the within-layer dyad universe would enumerate the impossible within-mode pairs as observed non-ties, so the fit is refused instead (statnet's bipartite layer terms b1dspL, b2dspL, ... are not implemented)\"; a single-layer statistic is expressed with WithinLayer(term, l) wherever ERGM.jl has the term.\nMultilevel statistics are descriptive. Nestedness, LevelHomophily and CrossLevelEdge on a MultilevelNetwork are evaluated with compute only and never enter a fit; ergm_multi(::MultilevelNetwork, ...) is a MethodError. What you get: the numbers, and nothing that looks like an estimate.\n\nOne smaller limitation of the estimator is documented where it arises in Model Estimation: the inverse-Hessian standard errors of a dyad-dependent model can be anticonservative. A model-based bootstrap can account for dependence under the fitted model, but requires adequate simulation and does not remove the approximation in the point estimator.","title":"Not implemented"},{"category":"section","location":"#References","page":"Home","text":"Krivitsky, P.N., Koehly, L.M. & Marcum, C.S. (2020). Exponential-family random graph models for multi-layer networks. Psychometrika, 85(3), 630-659.","title":"References"},{"category":"section","location":"#Citation","page":"Home","text":"If you use ERGMMulti.jl in your work, please cite it using the entry in CITATION.bib:\n\n@misc{SNWJERGMMultiJL,\n  author = {{Statistical Network Analysis with Julia}},\n  title = {ERGMMulti.jl: Exponential Random Graph Models for Multilayer Networks in Julia},\n  year = {2026},\n  url = {https://github.com/statistical-network-analysis-with-Julia/ERGMMulti.jl},\n  note = {Homepage: https://statistical-network-analysis-with-Julia.github.io/ERGMMulti.jl; GitHub: https://github.com/statistical-network-analysis-with-Julia}\n}","title":"Citation"},{"category":"section","location":"#Module","page":"Home","text":"","title":"Module"},{"category":"module","location":"#ERGMMulti","page":"Home","text":"ERGMMulti.jl - ERGMs for Multiple and Multilayer Networks\n\nFits ERGMs to multilayer network data (the same actors observed on several relations), following R ergm.multi (Krivitsky, Koehly & Marcum 2020): the layers form a block-diagonal combined network with layer membership attributes (combine_networks), the model's dyad universe is the set of within-layer dyads, and layer-aware terms carry per-layer, pooled, and cross-layer effects. Estimation (fit_ergm_multi, the R name ergm_multi being the same function) is by maximum pseudo-likelihood over the within-layer dyads with support for per-layer offset coefficients — there is no MCMLE (see \"Not implemented\" in the README and the documentation index); simulation (simulate_multi_ergm) uses the ERGM family's Metropolis kernel restricted to within-layer dyads.\n\nAlso provides containers for multiple independent networks (MultiNetwork) and descriptive statistics for multilevel designs (MultilevelNetwork); neither enters a fit.\n\n\n\n\n\n","title":"ERGMMulti"}]
}
